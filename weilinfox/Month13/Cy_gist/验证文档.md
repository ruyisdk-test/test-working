# 验证流程

```shell

docker run --rm -it --ulimit "nofile=1024:1048576" archlinux:base-devel
useradd -m -G wheel test-user
pacman -Sy
pacman -S nano
EDITOR=nano visudo # Uncomment %wheel ALL=(ALL:ALL) NOPASSWD: ALL
su - test-user
mkdir xingque && cd xingque

# 导入 PKGBUILD

makepkg -s
makepkg -i

python -c "import xingque"
```

import xingque 未见任何报错，说明安装成功。