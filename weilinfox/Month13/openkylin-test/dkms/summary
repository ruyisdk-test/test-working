run_test.sh          SKIP Test restriction "breaks-testbed" requires testbed capability "revert-full-system"
install_some-dkms_after_linux-headers SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
install_linux-headers_after_some-dkms SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
install_some-dkms_after_linux-image_without_linux-headers SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
install_linux-image_after_some-dkms_without_linux-headers SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
run_test.sh          SKIP Test restriction "breaks-testbed" requires testbed capability "revert-full-system"
install_some-dkms_after_linux-headers SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
install_linux-headers_after_some-dkms SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
install_some-dkms_after_linux-image_without_linux-headers SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
install_linux-image_after_some-dkms_without_linux-headers SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
