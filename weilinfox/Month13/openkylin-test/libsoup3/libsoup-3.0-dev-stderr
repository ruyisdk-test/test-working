+ mktemp -d
+ WORKDIR=/tmp/tmp.twoU5xUv3C
+ export XDG_RUNTIME_DIR=/tmp/tmp.twoU5xUv3C
+ trap rm -rf "$WORKDIR" 0 INT QUIT ABRT PIPE TERM
+ cd /tmp/tmp.twoU5xUv3C
+ [ -n  ]
+ CROSS_COMPILE=
+ cat
+ cflags=
+ code=soup.c
+ lib=libsoup-3.0
+ pkg-config --cflags --libs libsoup-3.0
+ gcc -o libsoup-3.0-test soup.c -I/usr/include/libsoup-3.0 -I/usr/include/glib-2.0 -I/usr/lib/riscv64-linux-gnu/glib-2.0/include -I/usr/include/sysprof-6 -I/usr/include/libmount -pthread -I/usr/include/blkid -lsoup-3.0 -Wl,--export-dynamic -lgio-2.0 -lgmodule-2.0 -pthread -lgobject-2.0 -lglib-2.0
+ echo build (libsoup-3.0): OK
+ [ -x libsoup-3.0-test ]
+ dbus-run-session -- xvfb-run -a ./libsoup-3.0-test
+ echo run (libsoup-3.0): OK
+ rm -rf /tmp/tmp.twoU5xUv3C
