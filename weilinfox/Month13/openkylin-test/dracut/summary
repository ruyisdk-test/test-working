upstream-dracut-core SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
upstream-dracut-live SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
upstream-dracut-network SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
upstream-dracut-core SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
upstream-dracut-live SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
upstream-dracut-network SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
