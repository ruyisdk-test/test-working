qemu-klibc           SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-busybox         SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-ata-only        SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-virtio-only     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-separate-usr    SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-panic-shell     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-net             SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-net-dnsmasq     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-klibc           SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-busybox         SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-ata-only        SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-virtio-only     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-separate-usr    SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-panic-shell     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-net             SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
qemu-net-dnsmasq     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
unit-tests           PASS
