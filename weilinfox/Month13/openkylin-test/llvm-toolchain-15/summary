command1             SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
integration-test-suite-test SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
command1             SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
integration-test-suite-test SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
cmake-test           FAIL badpkg
blame: llvm-toolchain-15_15.0.7-ok3.dsc
badpkg: Test dependencies are unsatisfiable. A common reason is that your testbed is out of date with respect to the archive, and you need to use a current testbed or run apt-get update or use -U.
command2             FAIL badpkg
blame: llvm-toolchain-15_15.0.7-ok3.dsc
badpkg: Test dependencies are unsatisfiable. A common reason is that your testbed is out of date with respect to the archive, and you need to use a current testbed or run apt-get update or use -U.
command3             PASS
