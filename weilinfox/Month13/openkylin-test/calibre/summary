smoke-calibre        SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-ebook-convert  SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-ebook-meta     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-ebook-polish   SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
convert-md-epub.sh   SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-calibre        SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-ebook-convert  SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-ebook-meta     SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
smoke-ebook-polish   SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
convert-md-epub.sh   SKIP Test lists explicitly supported architectures, but the current architecture riscv64 isn't listed.
